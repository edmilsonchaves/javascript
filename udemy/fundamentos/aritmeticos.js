const [a, b, c, d, e] = [10, 15, 22, 52, 100]

const soma = a + b + c + d + e
const subtracao = b - a
const multiplicacao = a * c
const divisao = e / d
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao.toFixed(2), modulo)

