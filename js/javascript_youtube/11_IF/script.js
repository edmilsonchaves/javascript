/*
    OPERADORES CONDICIONAIS

* São instruções que pode determinar o fluxo de uma aplicação.;
* Amplamentes utilizados, deixam os softwares dinâmicos;
* Exemplos: IF, ELSE IF, ELSE;

    CONDIÇÃO IF

* Executa um código de bloco se a condição for verdadeira.
* Se for falsa, segue o bloco normalmente;
* Lembrando que true e false, são do tipo BOOLEAN;

*/

console.log('Antes do IF')

if (true) {  // se o resultado for FALSE, não executa o comando dentro do bloco. 
    console.log('Testando o IF')
}

console.log('Depois do IF')

